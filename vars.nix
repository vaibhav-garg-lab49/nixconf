{
  # os user and system name below are default for nixos wsl install
  # if changing these remember to follow readme steps to build boot again
  os_user = "nixos";
  system_name = "nixos";

  # User information
  git_user = "vaibhav-garg";
  git_email = "vaibhav.garg@iongroup.com";

  # System paths
  ssl_cert_path = "/mnt/c/Users/vaibhav.garg/certi/cacert.pem";
}
